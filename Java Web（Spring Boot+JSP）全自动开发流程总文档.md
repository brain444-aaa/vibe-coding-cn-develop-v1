我将后端3步+前端6步（含所有优化、注释、缺口补充）整合为一份 **完整版总文档**，纯提示词、无多余说明，按开发流程排序，可直接复制保存，后续新建页面只需按顺序调用每一步提示词即可，实现前后端全自动生成闭环。

# Java Web（Spring Boot+JSP）全自动开发流程总文档
## 核心说明
本文档包含「后端开发3步+前端开发6步」，严格按「需求→分析→设计→开发→校验」逻辑排序，所有提示词可直接复制给AI使用，无需修改格式，确保生成的代码规范、可部署、前后端无缝对接。

---

## 第一部分：后端开发流程（3步）
### 【1】第一步：原始需求 → 拆解为增删改查+上传步骤（仅拆解）
```
你是Java Web需求拆解专员，专注将用户原始需求拆解为标准业务操作步骤。
任务：仅对我提供的【原始需求】进行拆解，不做任何额外分析、不写开发提示词，只输出拆解后的标准步骤。

拆解要求：
严格对应以下操作类型，有则写，无则不写，步骤清晰、简洁，贴合需求：
1.  列表查询（含分页、查询条件，明确条件字段）
2.  详情查看（明确查看的核心字段）
3.  新增操作（明确新增的字段、必填项）
4.  修改操作（明确修改的字段、可修改范围）
5.  删除操作（明确删除方式：单个删除/批量删除、是否需要二次确认）
6.  文件上传（如有，明确上传文件类型、大小限制、关联字段）
7.  导入/导出（如有，明确导入导出的字段、格式）
8.  状态变更（如有，明确状态类型、变更规则）

输出格式：
仅输出拆解后的步骤，无多余文字。

我的原始需求：
【在这里粘贴你的原始需求】
```

### 【2】第二步：拆解步骤 → 分析+审核+生成后端开发提示词
```
你现在同时担任两个角色：Java Web 需求分析师、Java Web 架构师。
任务：基于我提供的【需求拆解步骤】（即第一步的输出结果），完成分析、审核，最终生成可直接丢给AI开发后端的标准提示词。

第一步：担任【Java Web 需求分析师】
基于提供的【需求拆解步骤】，输出结构化业务分析：
1.  业务功能描述（结合拆解步骤，清晰梳理完整业务）
2.  涉及数据表（明确表名、字段、主键、外键、字段类型、长度、备注，贴合拆解步骤）
3.  后端类规划（明确类名、核心字段/方法，贴合拆解步骤）
    - Entity（对应数据表，字段与表字段一致）
    - DTO/VO（区分新增/修改/查询的字段，贴合拆解步骤）
    - Controller（类名、接口URL前缀，对应拆解的每一步操作）
    - Service/ServiceImpl（类名、核心业务方法，对应拆解步骤）
    - Mapper/Mapper XML（类名、核心SQL功能，对应拆解步骤）
4.  完整业务流程（前端→Controller→Service→Mapper→DB，贴合拆解的每一步操作）
5.  接口清单（每一个接口对应拆解步骤，明确请求方式、URL、入参、出参）
6.  校验规则、异常、日志、权限、分页（贴合拆解步骤，有则详细写，无则说明）

第二步：担任【Java Web 架构师】
对上面需求分析师的结果做专业审核，输出审核结论：
1.  需求拆解落地性：拆解步骤是否能完全覆盖，有无遗漏
2.  表结构合理性：有无冗余字段、缺失字段，关联是否合理
3.  分层设计规范性：Controller/Service/Mapper 职责是否清晰，符合Spring Boot+JSP规范
4.  业务流程通顺性：有无逻辑漏洞、步骤断层
5.  接口设计合理性：请求方式、URL、参数是否规范，贴合拆解步骤
6.  优化建议与修正结论（明确修改点，不冗余）

第三步：根据审核通过的结果，输出【可直接开发的后端标准提示词】
格式固定如下，不增删任何模块，内容贴合审核后的结果：
====================
【后端开发标准提示词】
技术栈：Spring Boot + MyBatis + JSP

一、业务需求
（基于审核后的业务功能描述）
二、数据表设计
（基于审核后的表结构）
三、后端类规划
（基于审核后的类规划）
四、业务流程
（基于审核后的业务流程）
五、接口清单
（基于审核后的接口清单）
六、校验与异常
（基于审核后的校验、异常规则）
七、统一返回格式
code（状态码）、message（提示信息）、data（返回数据），与项目规范一致
八、开发约束
1.  严格遵循项目包路径、命名规范（类名、方法名、字段名）
2.  事务、日志、异常处理与项目现有规范一致
3.  接口请求/返回格式贴合清单，适配JSP前端交互
4.  自动生成完整后端代码：Entity、Mapper、Mapper XML、Service、ServiceImpl、Controller
====================

请开始处理我的需求拆解步骤：
【在这里粘贴第一步输出的需求拆解步骤】
```

### 【3】第三步：后端开发提示词 → 生成代码+架构师安全审核
```
你是资深 Java Web 后端工程师 + Java 架构师。
任务分为两步：先写代码，再审核代码。

第一步：根据我提供的【后端开发标准提示词】，生成完整、可直接运行的后端代码。
要求：
1. 技术栈：Spring Boot + MyBatis + JSP
2. 代码严格规范：包结构、命名、注释、分层清晰
3. 安全严谨：参数校验、XSS防护、SQL注入防范、权限判断、异常处理
4. 覆盖所有场景：正常、异常、边界、空值、重复、越权
5. 输出完整代码：
   - Entity
   - Mapper 接口
   - Mapper XML
   - Service 接口
   - ServiceImpl
   - Controller
6. 统一返回格式、日志、异常、事务与项目一致

第二步：以 Java 架构师身份，对刚刚生成的代码进行专业审核：
1. 代码结构与分层是否合理
2. 业务逻辑是否正确、有无漏洞
3. 接口安全性：参数校验、越权、重复提交、注入风险
4. 异常处理、事务、日志是否规范
5. 代码可维护性、扩展性
6. 给出优化建议与最终结论

请接收我的【后端开发标准提示词】，并按要求执行：
【粘贴第二步生成的后端开发标准提示词】
```

---

## 第二部分：前端开发流程（6步）
### 【4】第四步：原始需求+原型图 → 页面结构+通用组件识别（只分析）
```
角色：资深JSP前端工程师，专注页面结构拆解、通用组件识别。
任务：仅分析结构、元素、功能、可复用组件，不写任何代码、不生成页面。

请根据我提供的【原始需求】和【原型图描述】，按以下结构输出分析结果：

### 页面整体功能
### 页面区块划分
### 各区块元素清单
### 交互功能清单
### 【通用组件识别清单】（类型 | 用途 | 建议搜索名）
### 页面独有功能
### 可能用到的公共JS/工具
### 前后端字段映射清单（前端name ↔ 后端Entity字段）
### 前后端接口初步约定（查询/新增/修改/删除 的URL、请求方式）

我的输入：
1. 原始需求：
【粘贴你的原始业务需求】
2. 原型图描述：
【粘贴原型图的布局、元素、交互说明，无则填“无”】
```

### 【5】第五步：公共组件 → 需求匹配度校验（只分析）
```
角色：资深JSP前端工程师，擅长组件适配性分析。
任务：分析我提供的公共组件是否满足当前页面需求，只输出判断与建议，不写代码。

### 输入内容：
1. 当前前端需求（结构+功能+交互）：
【粘贴第四步输出的完整分析结果】
2. 项目现有公共组件：
【名称】
【粘贴该组件的完整代码】

### 输出结构：
#### 组件功能说明
#### 匹配点（满足需求）
#### 不匹配/缺失点
#### 结论（可直接用 / 小改可用 / 不适用）
#### 修改建议（如需）
#### 使用注意事项
```

### 【6】第六步：页面样式设计思路（自适应+公共抽离+规范）（只分析）
```
角色：资深前端样式工程师，专注JSP后台、自适应、公共样式规划。
任务：仅输出样式设计思路与规则，不写CSS/JS代码、不实现页面。

### 输入内容：
1. 页面需求：
【粘贴原始需求+原型图描述】
2. 页面结构思路：
【粘贴第四步输出的结构】

### 样式约束：
- 支持窗口等比例缩放、自适应，少用固定像素
- 可复用样式/JS规划到独立公共文件
- 统一字体、颜色、按钮、表单、表格、间距、对齐、状态
- 不冲突全局样式、兼容浏览器、不溢出、不乱版
- 类名语义化、可扩展、符合后台管理系统风格

### 输出结构：
#### 整体布局思路
#### 自适应与缩放规则
#### 公共CSS抽离规划
#### 公共JS方法规划
#### 各区块样式规则
#### 状态样式（hover/聚焦/禁用/错误）
#### 类名与规范
#### 兼容性与注意事项
#### 扩展性设计
#### 项目静态资源路径规范
#### 数据渲染与回显规范
```

### 【7】第七步：页面样式设计（只输出HTML+CSS，预留JS相关名称）
```
角色：资深前端开发工程师，只做页面结构与样式，不写业务JS。
任务：根据前面的【页面结构】【样式思路】，输出 **纯净 HTML + CSS**，不包含业务逻辑 JS，
但必须 **预留好元素ID、class、JS方法名、表单name**，供后续JS直接使用。

### 输入：
1. 页面结构思路：
【粘贴第四步结果】
2. 样式设计规范：
【粘贴第六步结果】
3. 已校验公共组件：
【粘贴第五步可用组件】

### 要求：
1. 只输出：完整 HTML + 内嵌 CSS 或外部样式引用
2. 不写业务 JS、不写 AJAX、不写提交逻辑
3. 所有关键区域必须带 ID / class：
   - 查询表单：`id="queryForm"`
   - 查询按钮：`id="btnQuery"`
   - 新增按钮：`id="btnAdd"`
   - 修改按钮：`id="btnEdit"`
   - 删除按钮：`id="btnDelete"`
   - 表格：`id="dataTable"`
   - 分页：`id="pagination"`
   - 弹窗：`id="modalDialog"`
   - 弹窗表单：`id="editForm"`
4. 预留 JS 函数名（注释标明）：
   - 查询：`doQuery()`
   - 新增：`doAdd()`
   - 修改：`doEdit(id)`
   - 删除：`doDelete(id)`
   - 打开弹窗：`openModal()`
   - 关闭弹窗：`closeModal()`
   - 保存：`doSave()`
   - 表单校验：`validateForm()`
5. 自适应、等比缩放、符合后台管理系统风格
6. 表单 name 与后端 Entity 保持一致

输出：
【完整 HTML + CSS 代码】
```

### 【8】第八步：页面交互流程详细说明（只描述，不写代码）
```
角色：资深JSP前端交互设计师，专注后台页面交互逻辑梳理。
任务：根据【原始需求】【页面结构】【HTML+CSS页面】，输出完整的页面交互流程，
详细描述“用户点击操作→页面反馈→页面变化”，不写任何代码，只做结构化描述，
确保与上一步预留的ID、JS方法名对应，为后续编写JS逻辑提供明确依据。

【重要注释】：
所有交互优先保证**正常流程可用、功能完整**；
边界情况、异常处理、防重复、防错逻辑，**在主功能实现完成后再逐步补充**。

### 输入内容：
1. 原始需求 + 交互功能清单：
【粘贴原始需求 + 第四步的“交互功能清单”】
2. 页面结构与元素：
【粘贴第四步的“页面区块划分+各区块元素清单”】
3. 纯 HTML + CSS 页面（含预留ID/方法名）：
【粘贴第七步输出的HTML+CSS代码】

### 输出结构（按页面区块分类，详细描述每一个操作）：
#### 一、查询区交互
1. 点击查询按钮（id="btnQuery"）：
   - 触发操作：调用预留方法 doQuery()，提交查询表单（id="queryForm"）
   - 页面反馈：显示加载状态
   - 页面变化：表格区刷新，分页同步更新
2. 输入查询条件：
   - 触发操作：实时校验（validateForm()）
   - 页面反馈：错误提示
   - 页面变化：仅输入框状态变化

#### 二、操作按钮区交互
1. 点击新增按钮（id="btnAdd"）：
   - 触发操作：doAdd()、openModal()
   - 页面反馈：弹窗显示，标题“新增XXX”
   - 页面变化：表单清空
2. 点击修改按钮（id="btnEdit"）：
   - 触发操作：doEdit(id)、openModal()
   - 页面反馈：弹窗显示，标题“修改XXX”
   - 页面变化：表单回显数据
3. 点击删除按钮（id="btnDelete"）：
   - 触发操作：doDelete(id)
   - 页面反馈：二次确认
   - 页面变化：确认后删除行并刷新

#### 三、表格区交互
1. 点击表格行：
   - 触发操作：选中当前行
   - 页面反馈：高亮选中行
   - 页面变化：修改/删除按钮可用
2. 表头排序（如有）：
   - 触发操作：按字段排序
   - 页面反馈：排序图标变化
   - 页面变化：表格重新排序

#### 四、分页区交互
1. 点击页码：
   - 触发操作：doQuery() 带页码
   - 页面反馈：页码高亮
   - 页面变化：表格刷新对应页
2. 上一页/下一页：
   - 触发操作：切换页码
   - 页面反馈：禁用状态控制
   - 页面变化：表格刷新

#### 五、弹窗区交互
1. 关闭弹窗：
   - 触发操作：closeModal()
   - 页面反馈：弹窗隐藏
2. 保存按钮：
   - 触发操作：validateForm() → doSave()
   - 页面反馈：校验提示、加载中
   - 页面变化：成功则关闭弹窗并刷新列表
3. 取消按钮：
   - 触发操作：closeModal()
   - 页面变化：关闭弹窗

#### 六、其他交互（上传/状态切换等）
按实际需求补充。

---

#### 七、需要补充的边界与异常场景（先实现主功能，再完善以下内容）
1. 未选中任何数据时，点击【修改/删除】：给出提示，不执行操作
2. 表单重复提交：按钮置灰、防重复提交控制
3. 查询无数据：页面显示“暂无数据”
4. 网络/接口异常：给出统一错误提示
5. 加载中状态：防止用户重复操作
6. 表单校验不通过：聚焦错误项，不提交
7. 分页边界：第一页时“上一页”禁用，最后一页时“下一页”禁用

### 额外要求：
1. 所有交互必须与上一步 HTML+CSS 预留的 ID、class、JS 方法名完全对应
2. 描述清晰：点击什么 → 触发什么 → 显示什么 → 变化什么
3. 覆盖全部核心操作，不遗漏
4. 遵循注释原则：先保证功能正常运行，再补充边界与异常
```

### 【9】第九步（最终）：结构+样式+交互+组件 → 生成可部署JSP
```
角色：资深JSP前端工程师，严格按项目风格生成可直接运行页面。
任务：基于 **HTML+CSS页面** + **结构** + **组件** + **样式规范** + **交互流程**，生成完整可部署 JSP。

### 输入内容：
1. 页面结构思路：
【第四步结果】
2. 已验证公共组件：
【第五步结果】
3. 样式设计规范：
【第六步结果】
4. 纯 HTML + CSS 页面：
【粘贴第七步输出的页面】
5. 页面交互流程说明：
【粘贴第八步输出的交互流程】
6. 前端业务需求 + 交互：
【原始需求 + 第四步的“交互功能清单”】

### 硬性约束：
- JSP + JSTL + EL + jQuery
- 复用公共组件，不重复造轮子
- 与后端交互：表单 / Model / 跳转
- 不引入不存在的 CSS/JS
- 表单 name 必须与后端 Entity 一致
- 静态资源必须用 ${pageContext.request.contextPath}
- 必须包含：查询、新增、修改、删除、弹窗、校验、提示
- 直接使用第七步预留的 ID、class、JS 方法名，严格遵循第八步的交互流程
- 字段、路径不确定标注【待确认】

### 输出结构：
#### 【1 页面头部 include、CSS/JS 引入】
#### 【2 HTML主体结构】
#### 【3 JSTL数据渲染、回显、循环】
#### 【4 JS逻辑（查询、提交、校验、弹窗、事件）】（严格对应第八步交互流程）
#### 【5 页面使用注意事项】
```

---

## 开发流程执行顺序（必遵循）
1.  后端第一步 → 需求拆解
2.  后端第二步 → 分析+审核+生成后端开发提示词
3.  后端第三步 → 生成后端代码+架构师审核
4.  前端第四步 → 需求+原型 → 结构+组件识别
5.  前端第五步 → 公共组件匹配校验
6.  前端第六步 → 样式设计思路
7.  前端第七步 → 生成HTML+CSS纯页面
8.  前端第八步 → 交互流程详细说明
9.  前端第九步 → 生成最终可部署JSP

## 注意事项
1.  每一步的输入，均为上一步的输出结果，确保上下文连贯；
2.  所有提示词均适配Spring Boot+JSP技术栈，无需额外修改；
3.  严格遵循“先功能、后异常”的原则，提升开发效率；
4.  字段名、ID、方法名全程统一，避免前后端对接报错。

这份总文档已经包含所有优化和补充内容，按流程顺序排列好了。需要我帮你**在文档末尾添加一份简易执行对照表**（每一步输入/输出对应表），让你后续使用时更清晰吗？