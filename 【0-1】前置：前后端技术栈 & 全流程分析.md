下面是**纯提示词、可直接复制给通义灵码**，已经按你要求：
拆成 **后端分析 + 前端分析 + 前后端交互衔接** 三份，上下文干净、AI 分析更准。

---

## 【0-1】前置：后端技术栈 & 全流程分析（只分析后端）
```
你是资深Java Web后端架构师，擅长从后端代码反推技术栈、规范、分层逻辑、数据流转全流程。
任务：只分析我提供的后端代码，不写代码、不实现、不涉及前端内容，输出结构化完整结论。

我提供的后端代码：
【Controller】
【Service/ServiceImpl】
【Mapper】
【Mapper XML】
【Entity/VO/DTO】
【后端公共类/工具/异常/常量】

请按以下结构输出：

1. 后端技术栈
   - 核心框架（Spring Boot、Spring MVC、MyBatis）
   - 数据库、ORM、事务方式
   - 日志、异常、校验框架
   - 构建工具、包管理规范

2. 后端分层规范
   - 包结构规范
   - 类/方法/参数/表/字段命名规范
   - 统一返回格式（JSON/页面跳转）
   - 异常处理规范
   - 日志规范

3. 后端内部数据传递全流程
   - Controller 如何接收请求与参数
   - Controller → Service 传递什么、怎么传
   - Service → Mapper 传递什么、怎么传
   - Mapper → 数据库执行逻辑
   - 结果如何从数据库一层层返回前端

4. 业务逻辑详细解析
   - 核心模块与功能
   - 完整后端处理流程
   - 业务约束、校验、状态规则
   - 公共逻辑（分页、权限、导出等）

5. 后端公共资源
   - 工具类、常量、异常、通用组件用途与复用规则

6. 不确定点【待确认】
```

---

## 【0-2】前置：前端技术栈 & 全流程分析（只分析前端）
```
你是资深JSP前端工程师，专注从JSP/JS/CSS/组件分析前端规范、交互、渲染流程。
任务：只分析我提供的前端代码，不写代码、不实现、不涉及后端内部逻辑。

我提供的前端代码：
【JSP页面】
【公共JS】
【公共CSS】
【前端公共组件（表格/分页/下拉/单选/多选/切换）】

请按以下结构输出：

1. 前端技术栈
   - JSP/JSTL/EL 用法
   - jQuery、AJAX 封装方式
   - 样式规范、布局方式
   - 静态资源路径与引入规范

2. 前端规范
   - 文件命名、路径规范
   - 表单 name/id、CSS 类名、JS 函数命名
   - 页面结构、include 规范
   - 样式与 JS 编写规范

3. 前端交互 & 数据渲染全流程
   - 前端如何发起请求（GET/POST/AJAX/表单）
   - 传参格式、字段、校验规则
   - 前端如何接收后端数据
   - 如何渲染列表、回显表单、分页

4. 页面结构与组件分析
   - 页面区块（查询区/列表/按钮/弹窗/分页）
   - 公共组件用途、用法、交互
   - 页面独有功能与逻辑

5. 前端公共资源
   - 公共JS、公共CSS、公共组件复用规则

6. 不确定点【待确认】
```

---

## 【0-3】前置：前后端交互衔接分析（打通两端）
```
你是资深Java Web架构师，专门梳理前后端数据传递完整链路。
任务：只分析前后端交互，不写代码、不实现。

我提供的交互相关代码：
【JSP 请求与接收逻辑】
【Controller 接收与返回逻辑】
【AJAX/表单提交 JS】

请按以下结构输出：

1. 前后端交互整体规则
   - 请求方式（GET/POST）约定
   - 接口路径约定
   - 数据传递格式（表单/JSON/路径参数）

2. 前后端数据传递全链路
   - 前端 → 后端：传什么、字段、格式、校验
   - 后端 → 前端：返回什么、格式、渲染方式
   - 字段名对应关系
   - 参数/返回值类型对应关系

3. 典型场景完整流程（至少2～3个）
   示例：查询列表、提交表单、分页、删除、编辑回显

4. 异常交互流程
   - 后端异常 → 前端提示
   - 请求失败/参数错误处理
   - 数据回显异常流程

5. 不确定点【待确认】
```

